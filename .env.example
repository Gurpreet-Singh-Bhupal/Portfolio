# EmailJS — copy to `.env` and fill with your keys (never commit `.env`)
# Setup steps: docs/Resume/README.md
# Connect the EmailJS service to your Gmail so Contact form messages arrive there.

VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=
